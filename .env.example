# Vercel Blob Storage Configuration
# Get your token from: https://vercel.com/dashboard/stores

# Required: Vercel Blob Read/Write Token
BLOB_READ_WRITE_TOKEN=your_blob_read_write_token_here

# Local Development Configuration
# For onUploadCompleted callback to work locally, you need to use a tunneling service
# like ngrok to expose your local development server to the internet.
#
# Example setup:
# 1. Install ngrok: npm install -g ngrok
# 2. Run your development server: pnpm dev
# 3. In another terminal: ngrok http 3000
# 4. Copy the ngrok URL and set it below (uncomment the line)
#
# VERCEL_BLOB_CALLBACK_URL=https://your-ngrok-id.ngrok-free.app

# Production Environment Variables (automatically set by Vercel)
# These are automatically configured when deployed to Vercel - do not set manually
# VERCEL_PROJECT_PRODUCTION_URL=your-production-domain.vercel.app
# VERCEL_BRANCH_URL=your-branch-url.vercel.app
# VERCEL_URL=your-deployment-url.vercel.app
